# 电缆长度自动统计 — 快速上手（最简版）

照下面的顺序做，每一步做完再做下一步。详细解释见《使用说明.md》。

一个工程项目 = 一个项目文件夹（清册、CAD 数据、结果都在里面）；本文件夹只是通用工具，不放项目数据。

---

## 第零步：建项目文件夹

双击本文件夹的 `电缆统计.exe`，点「新建项目」，选位置、输项目名，自动建好并打开。
（也可以双击 `新建项目.cmd`，或直接把 `项目模板` 文件夹复制到任何地方改名。）

然后把 `自动统计.xlsx` 清册（表头含 起点、终点、电缆长度、电缆编号）放进项目文件夹：

- 表头里的空格、换行不用管，程序自动忽略；`自动统计`、`向上取整` 列没有也行，会自动补建。
- 保存时停在清册那张工作表上（程序只读活动工作表）。

---

## 最省事的做法：用引导向导（推荐）

建好项目文件夹后，CAD 里的活不想手动做的话：

1. 在 CAD 命令行输入 `APPLOAD`，加载本文件夹里的 `cad_cable_wizard.lsp`。
2. 输入命令 `DDFD_CABLE_WIZARD`，回车，照命令行提示一步步做：
   自动建图层 → 定义房间范围 → 点柜子位置、输柜名 → 画走线路径（自动开端点捕捉）→ 标竖井（自动写 `ZJn_楼层` 配对文字）→ 自动导出 CSV、复制进**项目文件夹**的 `data\` 并直接运行统计。
3. 中途退出没关系，重新输入 `DDFD_CABLE_WIZARD` 可以从任意一步继续；也可以直接用分步命令：
   `DDFD_CABLE_LAYERS`（建图层）/ `DDFD_CABLE_ROOMS`（定义房间）/ `DDFD_CABLE_CABINETS`（标柜子）/ `DDFD_CABLE_ROUTES`（画路径）/ `DDFD_CABLE_SHAFTS`（标竖井）/ `DDFD_CABLE_EXPORT_RUN`（导出并运行）。

用向导时第一到第七步都不用手动做，直接跳到**第八步：看结果**。下面的手动步骤留作对照和排查用。

---

## 第一步：在 CAD 图里建图层

打开工程 CAD 图，新建这些图层（有几层楼建几层，这里以两层为例）：

```text
CABLE_CABINET_1F    一层柜子出线点
CABLE_CABINET_2F    二层柜子出线点
CABLE_ROUTE_1F      一层电缆沟/桥架中心线
CABLE_ROUTE_2F      二层电缆沟/桥架中心线
CABLE_SHAFT_1F      一层竖井
CABLE_SHAFT_2F      二层竖井
CABLE_ROOM_1F       一层房间范围
CABLE_ROOM_2F       二层房间范围
```

## 第二步：定义房间范围

运行 `DDFD_CABLE_ROOMS`，逐个选择已闭合的直线多段线并输入房间名称。程序会复制边界到 `CABLE_ROOM_nF` 图层，原建筑图形不会改变。范围必须是闭合、无圆弧的多段线。

## 第三步：标柜子

在 `CABLE_CABINET_1F` / `CABLE_CABINET_2F` 图层上，在每个柜子的柜后出线点位置写一个**单行文字**，文字内容就是柜子名称。

- 柜名要和 Excel 清册里的 `起点` / `终点` 写得一样（空格差异不用管，程序会自动忽略）。

## 第四步：画走线路径

在 `CABLE_ROUTE_1F` / `CABLE_ROUTE_2F` 图层上，用**直线或多段线**沿电缆沟/桥架中心画线。

- 电缆能走哪里，线就画到哪里。
- 两条线相接时，**端点必须真正搭上**（端点对端点，或端点搭在另一条线上），只是看起来交叉不算连通。

## 第五步：标竖井（不跨楼层可跳过）

在 `CABLE_SHAFT_1F` / `CABLE_SHAFT_2F` 图层上，在竖井位置各写一个文字，同一个竖井上下层名字要配对：

```text
一层写 ZJ1_1F
二层写 ZJ1_2F
```

## 第六步：从 CAD 导出数据

1. 在 CAD 命令行输入 `APPLOAD`，加载软件导出的 `cad_cable_wizard.lsp`。
2. 输入命令 `DDFD_EXPORT_CABLE_ROUTE`，回车。
3. 导出完成后，在 DWG 所在目录会生成 `cable_export` 文件夹，里面有 4 个文件：
   `柜子坐标.csv`、`路径线段.csv`、`竖井.csv`、`房间范围.csv`。
4. 把这 4 个文件复制到**项目文件夹**的 `data\` 里，覆盖同名文件。

## 第六步：放好 Excel 清册

清册应该在第零步已经放进项目文件夹了，没放的现在放。

## 第七步：运行

双击**项目文件夹**里的：

```text
一键启动.cmd
```

## 第八步：看结果

打开项目文件夹的 `outputs\自动统计_计算结果.xlsx`：

- `Sheet1`：`自动统计` 列已自动填好长度，右边的 `向上取整` 列是取整后的整数（18.2 → 19）。
- `问题清单`：看有没有柜子缺坐标、路径不通等问题。

再双击 `outputs\路径可视化.html`，点某条电缆，检查高亮路径走得对不对。

## 第九步：处理对不上的柜名（有问题才做）

如果 `问题清单` 里有柜子找不到：

1. 打开 `outputs\柜子清单.csv`，看 `近似CAD柜名建议` 列。
2. 把对应关系填进 `data\柜名别名.csv`（格式：`清册名称,CAD名称,备注`）。
3. 重新双击 `一键启动.cmd`。

---

## 计算规则（了解即可）

- 路径不拐弯：CAD 最短路径 + 7 米。
- 路径拐弯或跨楼层：(CAD 最短路径 + 7 米) × 1.2，跨层再加竖井高度（默认 4.5 米）。
- 想改这些数值：编辑 `data\参数.csv`。
- 某条电缆想强制指定修正量：填 `data\强制规则.csv`。
- 只要路径进入过房间但没有始终位于同一个房间内，额外增加 `跨房间修正`，默认 3 米；完全位于房间外的路径不增加。该值可在 `data\参数.csv` 调整。


## 房间导出范围（2026-09-20 修复）

导出时首次默认选择房间，请框选当前工程各楼层所需的房间边界；直接回车不再全图导出。成功后在项目 `data/房间导出范围.txt` 保存图纸路径和房间句柄，下次默认复用。换图纸、房间被删除或边界失效时必须重新选择；增加房间后选 S 更新范围。选 A 全图导出需再输入 Y 确认，选 N 表示本次明确不使用房间并清空房间 CSV。

向导先确认项目，再直接写入该项目 data 目录，不再从公共 cable_export 目录复制旧数据。取消选择不覆盖 CSV，也不启动统计。四份 CSV 先生成临时文件，再成组替换；替换失败时恢复备份。若提示 `.previous` 文件，请检查上次失败的备份，不要直接删除。

更新插件后需在 CAD 重新加载项目目录中的 `cad_cable_wizard.lsp` 才会生效。程序不会删除 CAD 中的旧房间实体，也不会按距离猜测并删除有效房间。
