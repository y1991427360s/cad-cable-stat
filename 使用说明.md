# 电缆长度自动统计工具使用说明

## 一、工具目标

本工具用于把 `自动统计.xlsx` 里的 `起点`、`终点` 自动换算成 `自动统计` 长度。

计算逻辑：

```text
自动统计 = 按规则修正后的 CAD 固定路径最短长度
```

当前规则：

- 路径不拐弯：加 `7m`
- 路径涉及拐弯或跨楼层：先加 `7m`，再乘以 `1.2`
- 一层到二层通过电缆竖井：把竖井高度计入路径长度，本项目默认 `4.5m`
- 结果保留 1 位小数，最后人工决定怎么取整
- 柜子到电缆沟/桥架按 CAD 实际距离计算
- 同一个柜子默认从柜后一个出线点出线

## 二、目录说明

工具和项目**分开存放**：本文件夹是通用工具，不放项目数据；每个工程项目一个独立文件夹（清册、CAD 数据、结果都在里面），项目文件夹可以放在任何位置。

```text
电缆长度自动统计工具（本文件夹，通用）
├─ 电缆统计.exe                        主程序（图形界面，双击即用，无需装Python；
│                                       内嵌计算引擎、项目模板和CAD插件）
├─ cable_stat_app.py                   主程序源码（GUI）
├─ calculate_cable_lengths.py          Excel计算脚本
├─ run_auto_stat.py / 运行自动统计.ps1  源码命令行入口（开发/排查用）
├─ cad_export_cable_route.lsp          仅供开发兼容的精简导出源码（用户无需加载）
├─ cad_cable_wizard.lsp                CAD分步引导向导（含导出命令，推荐加载这个）
├─ test_calculate_cable_lengths.py     轻量测试
├─ 新建项目.cmd                        双击创建新项目文件夹
├─ 项目模板\                           新项目骨架（也可直接复制到任何地方改名）
└─ 项目\
   └─ <项目名>\                       一个项目 = 一个文件夹
      ├─ 自动统计.xlsx                 原清册放这里（不会被覆盖）
      ├─ 一键启动.cmd                  双击运行本项目
      ├─ data
      │  ├─ 参数.csv                   可调参数（单位换算、修正量等）
      │  ├─ 柜子坐标.csv               CAD导出的柜子坐标（兼容旧名 cabinets.csv）
      │  ├─ 路径线段.csv               CAD导出的固定路径线段（兼容旧名 route_segments.csv）
      │  ├─ 竖井.csv                   CAD导出的竖井坐标（兼容旧名 shafts.csv）
      │  ├─ 房间范围.csv               CAD导出的房间多边形顶点（可选）
      │  ├─ 柜名别名.csv               清册名称→CAD名称映射（人工维护）
      │  └─ 强制规则.csv               按起点+终点覆盖修正量（人工维护）
      └─ outputs
         ├─ 自动统计_计算结果.xlsx
         ├─ 统计明细.csv
         ├─ 柜子清单.csv
         └─ 路径可视化.html
```

新建项目：用 `电缆统计.exe` 的「新建项目」按钮（可建在任何位置），或双击工具文件夹的 `新建项目.cmd` 输入项目名（建在 `项目\` 下并自动打开），或把 `项目模板` 文件夹复制到任何地方改名。项目里的 `一键启动.cmd` 按绝对路径启动工具文件夹的 `电缆统计.exe`；工具文件夹挪位置后要改各项目 `一键启动.cmd` 里的 exe 路径。

清册要求：脚本在**项目文件夹**里查找活动工作表表头包含 `起点、终点、电缆长度` 的工作簿，并优先使用文件名为 `自动统计.xlsx` 的那个。表头匹配会忽略空格和单元格内换行（设计院清册常见的「电缆↵长度」写法能直接识别）；`自动统计`、`向上取整` 列可以没有，缺了会自动补建（`向上取整` 固定补在 `自动统计` 右边；清册还需有 `电缆编号` 列）。保存清册时要停在清册那张工作表上，程序只读活动工作表。

注意：

- 项目文件夹里可以有其他 Excel 文件，只要表头不同就不会被误选；如果有多个表头符合的工作簿，脚本会报错提示只保留一个或改名为 `自动统计.xlsx`。
- 原清册不会被覆盖，结果输出到项目自己的 `outputs\` 里。

## 二点五、CAD 分步引导向导（推荐先用）

`APPLOAD` 加载 `cad_cable_wizard.lsp` 后输入 `DDFD_CABLE_WIZARD`，就能照命令行提示把第三、四节的准备工作和导出一次做完：

1. **建图层**：输入楼层数，自动创建每层的 `CABLE_CABINET_nF`（黄）/ `CABLE_ROUTE_nF`（青）/ `CABLE_SHAFT_nF`（红），已存在的不重建。
2. **标柜子**：逐层提示"点出线点 → 输柜名"，自动在对应图层写单行文字；柜名为空则该点作废，标错用 `ERASE` 删掉文字重标。
3. **画路径**：自动切到路径图层并打开端点/交点/最近点捕捉，反复调用多段线命令，每画完一条问是否继续。
4. **标竖井**：输入竖井编号（默认 `ZJ1`、`ZJ2` 递增）和起止楼层，在每层平面图上点一下位置，自动写 `ZJ1_1F` / `ZJ1_2F` 这类配对文字；只有一层楼会自动跳过。
5. **导出并运行**：自动导出 4 个 CSV 到 DWG 目录的 `cable_export\`，再复制到**项目文件夹**的 `data\`（项目路径会记住，下次直接回车），最后可以直接帮你运行该项目的 `一键启动.cmd`。

向导入口是一个**带说明的菜单**：每个步骤旁边写着它是干什么的，看清楚再选；单步做完会自动回到菜单继续选下一步，选 `Q`（或回车）退出，点错了不用重新输命令。

向导会记住楼层数和项目文件夹（存在 CAD 环境项 `DDFD_CABLE_FLOORS` / `DDFD_CABLE_PROJDIR` 里）。中途 ESC 退出不影响已画内容，重新输入 `DDFD_CABLE_WIZARD` 选步骤号即可继续；各步也有独立命令：`DDFD_CABLE_LAYERS`、`DDFD_CABLE_ROOMS`、`DDFD_CABLE_CABINETS`、`DDFD_CABLE_ROUTES`、`DDFD_CABLE_SHAFTS`、`DDFD_CABLE_EXPORT_RUN`。该文件同时包含 `DDFD_EXPORT_CABLE_ROUTE`，不需要再加载 `cad_export_cable_route.lsp`。

用向导时，下面第三、四节只作对照和排查参考。

## 三、CAD图层约定

建议在 CAD 中新增或整理以下图层。

### 1. 柜子出线点

图层：

```text
CABLE_CABINET_1F
CABLE_CABINET_2F
```

做法：

- 在每个柜子“柜后出线点”放一个文字、点或块。
- 最推荐用文字或带属性块，文字内容就是清册里的柜子名称。
- 柜名必须尽量和 Excel `起点` / `终点` 一致。
- 柜名里的空格会在 CAD 导出和统计读取时自动剔除，例如 `10kV #1 主变进线柜` 会按 `10kV#1主变进线柜` 匹配。

示例：

```text
时间同步柜
I区数据通信网关机柜
10kV#1主变进线柜
```

### 2. 固定走线路径

图层：

```text
CABLE_ROUTE_1F
CABLE_ROUTE_2F
```

做法：

- 用 `LINE` 或 `LWPOLYLINE` 画电缆沟/桥架中心线。
- 电缆实际能走哪里，线就画到哪里。
- 转弯处必须是多段线顶点，或者两条线的端点重合。
- T型连接：一条线的端点搭在另一条线中间（在吸附容差内）也会自动连通。
- 如果两条路径只是视觉相交，但端点没有搭上去，程序不会默认认为可连通。

### 3. 电缆竖井

图层：

```text
CABLE_SHAFT_1F
CABLE_SHAFT_2F
```

做法：

- 在一层、二层竖井位置各放一个文字、点或块。
- 同一个竖井的一层和二层名称要能对应。

推荐命名：

```text
ZJ1_1F
ZJ1_2F
ZJ2_1F
ZJ2_2F
```

程序会把 `ZJ1_1F` 和 `ZJ1_2F` 配成一组竖井。

## 四、从 CAD 导出

1. 打开包含柜子和电缆路径的 CAD 图。
2. 执行 `APPLOAD`，加载：

```text
本文件夹\cad_cable_wizard.lsp
```

3. 执行命令：

```text
DDFD_EXPORT_CABLE_ROUTE
```

4. 选择导出目录，默认会在当前 DWG 所在目录生成：

```text
cable_export\柜子坐标.csv
cable_export\路径线段.csv
cable_export\竖井.csv
cable_export\房间范围.csv
```

5. 把这四个 CSV 复制到项目文件夹的 `data\`，覆盖同名文件（用向导的第5步则自动完成复制）。

## 五、检查柜子坐标

`柜子坐标.csv` 里每个柜子必须有名称、楼层和坐标。

长度规则只看 CAD 路径是否拐弯或跨楼层：不拐弯加 `7m`，拐弯或跨楼层先加 `7m` 再乘以 `1.2`。

示例：

```csv
柜子名称,楼层,X,Y,图层,对象类型,句柄
时间同步柜,2F,12500,8300,CABLE_CABINET_2F,TEXT,1A2B
I区数据通信网关机柜,2F,16700,8300,CABLE_CABINET_2F,TEXT,1A2C
```

## 六、修改项目参数

打开：

```text
data\参数.csv
```

常用参数：

```text
CAD每米单位 = 1000     默认按毫米图；米图填 1，厘米图填 100
竖井高度 = 4.5         跨楼层经竖井额外加的高度，单位米
不拐弯修正 = 7         路径不拐弯时加的余量，单位米
拐弯倍率 = 1.2         拐弯或跨楼层时先加余量再乘的倍率
吸附容差 = 0.05        CAD图纸单位；两个端点距离在此范围内视为连通
最大接入距离 = 20000   CAD图纸单位；柜子/竖井到最近路径超过此值报错
```

`吸附容差`、`最大接入距离` 的单位跟图纸一致：米图用 `0.05` / `20` 这个量级，厘米图放大 100 倍（如 `5` / `2000`），毫米图放大 1000 倍（如 `50` / `20000`）。

不知道图纸是什么单位？看坐标数值量级，或先随便跑一遍——参数体检会提示路径总长是否合理。

文件不存在时脚本使用内置默认值；内置值和 `项目模板\data\参数.csv` 均默认按毫米图配置，每个项目仍可按图纸单位调整。

**不确定参数怎么调？直接跑一遍**：程序会自动做参数体检，发现参数和图纸数据不匹配时，在运行日志和「问题清单」里给出以「参数提示」开头的具体建议——吸附容差比最短线段还大会警告并给建议值；柜子超出最大接入距离会告诉你实际最远多少、该改成多少；单位换算后路径总长明显不合理会提示核对 `CAD每米单位`。照着改完再跑即可。

## 六点五、柜名别名表与强制规则表

### 柜名别名表 `data\柜名别名.csv`

当 CAD 文字和 Excel 柜名不完全一致（比如简写、别名）时，不用改图，直接加一行映射。单纯空格差异会自动忽略，不需要写别名：

```csv
清册名称,CAD名称,备注
10kV#1主变进线柜,10kV一号主变进线柜,CAD文字是别名
```

填写来源：打开 `outputs\柜子清单.csv`，缺坐标的柜子会在 `近似CAD柜名建议` 列给出最接近的 CAD 柜名，确认后抄进别名表即可。

### 强制规则表 `data\强制规则.csv`

某条电缆不想按自动判断的 +7 或先 +7 再 ×1.2 时，按起点+终点强制指定修正量（起点终点写反也能命中）：

```csv
起点,终点,修正量,备注
主变保护柜1,主变测控柜1,7,现场确认走直线
```

## 七、运行统计

三种方式任选：双击工具文件夹的 `电缆统计.exe`（图形界面里选项目、点「开始计算」）；双击**项目文件夹**里的 `一键启动.cmd`（打开软件并自动对该项目开算）；或在工具文件夹用 PowerShell 指定项目运行：

```powershell
.\运行自动统计.ps1 -Project "<项目文件夹>"
```

输出文件（都在项目文件夹里）：

```text
outputs\自动统计_计算结果.xlsx
outputs\路径可视化.html
outputs\路径可视化数据.json
```

重点查看：

- `Sheet1`：原清册，已填 `自动统计`，右边一列 `向上取整` 是同一数值向上取整后的整数（18.2 → 19）；两列都缺失时自动补建，`向上取整` 固定补在 `自动统计` 右边（右边已有其他列会插入新列、原有列右移）
- `统计明细`：每条电缆的计算路径、规则、差值、失败原因
- `柜子清单`：清册中出现的全部柜子，检查哪些还没提供坐标
- `问题清单`：汇总缺坐标、路径不连通等问题
- `路径可视化.html`：双击打开，左侧选择电缆，中间查看高亮路径，右侧查看基础长度、修正量和路径步骤；顶部可切换「平面 / 三维」视图，三维视图按竖井把楼层对齐叠放，勾选「全部电缆」可一次看到所有电缆走向。鼠标操作与 CAD 一致：三维视图中键拖动平移、Shift+中键围绕鼠标下方的柜子/竖井/路径点旋转（左键旋转、右键平移同样可用）、滚轮缩放、双击复位、点击电缆线选中；平面视图中键拖动平移、滚轮缩放、双击恢复自适应
- `路径可视化数据.json`：给 HTML 页面使用的结构化路径数据，可供后续二次开发

## 八、第一轮校验建议

第一次不要追求一次性全自动准确。建议流程：

1. 先只画一条主路径或一组典型路径。
2. 导出并运行统计。
3. 在 `统计明细` 里筛选 `状态 = OK`。
4. 对比 `自动统计` 和手工 `电缆长度`。
5. 把差值大的行拿出来看路径是否画错、柜位是否放错、竖井是否没配对。

等 20 到 50 条典型电缆偏差合理后，再扩展到全站。

## 九、常见问题

### 1. 柜子名称找不到

原因通常是 CAD 文字和 Excel 柜名存在空格以外的差异，例如简写、错别字或编号不同。单纯空格差异会自动忽略。

处理：

- 打开 `outputs\柜子清单.csv`，看 `近似CAD柜名建议` 列。
- 确认后把映射写进 `data\柜名别名.csv`，不用改 CAD 图。
- 也可以直接把 CAD 文字改成和 Excel 一致。

### 2. 路径网络不连通

原因通常是路径线端点没有真正重合，或者跨楼层竖井没有配对。

处理：

- 检查转弯和分支处是否有顶点。
- 检查线段端点是否在 `吸附容差` 范围内。
- 检查 `ZJ1_1F` / `ZJ1_2F` 这类竖井命名是否对应。

### 3. 明明是直线却没按 +7

原因通常是 CAD 路径中间存在方向变化，或者路径经过竖井被判定为跨楼层。

处理：

- 检查固定路径是否有多余折点。
- 检查起点到终点是否经过了竖井或跨楼层连接。

### 4. 路径拐弯判断不符合人工习惯

当前判断是：路径方向发生明显变化，或者经过竖井，就认为拐弯。

如果某些起终点要按特殊修正量算，在 `data\强制规则.csv` 里按起点+终点指定，覆盖自动判断。


## 房间识别规则（2026-09-23）

- **只有用“定义房间”（`DDFD_CABLE_ROOMS` 或向导菜单 R）写过名称的多段线才算房间。** `CABLE_ROOM_nF` 图层上没有名称的多段线（误放的图框、表格等）一律不导出，导出时命令行会提示有几个。
- 导出不再询问范围，直接扫描模型空间里的命名房间，并在命令行按楼层列出（如 `1F 10`、`2F 2DR`），数量不对当场就能看出来；同层重名也会提示。旧版的 `data\房间导出范围.txt` 已不再使用，下次导出时自动删除。
- 房间数量不对时输入 `DDFD_ROOM_CHECK`（向导菜单 C）：自动选中并缩放到房间图层上未命名、或已命名但不闭合/含圆弧/图层看不出楼层的多段线，只选中不修改。确认是图框/表格后在特性里改回原图层或删除；也可输入 `DDFD_CLEAN_ROOM_LAYERS` 把未命名的统一移到 0 层（先列出句柄并确认，输入 `U` 可撤销）。
- 定义房间不会切换当前图层，之后画的图形不会误落到房间图层；再次选中已定义的房间可直接改名，同层重名会先询问是否替换旧的。
- 统计端兜底：房间里既没有柜子也没有路径经过、或房间没有名称时，在结果工作簿“问题清单”和运行日志里提示。

四份 CSV 先生成临时文件，再成组替换；任一图元读取失败会报出句柄并放弃本次导出，不覆盖原数据。若提示 `.previous` 文件，请检查上次失败的备份，不要直接删除。

插件加载时会打印版本号（如 `版本 2026-09-23`）。更新插件后需在 CAD 重新加载项目目录中的 `cad_cable_wizard.lsp`，看到新版本号才算生效。
